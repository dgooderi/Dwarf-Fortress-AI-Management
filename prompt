TOTAL MANAGEMENT PROTOCOL
[1] CONFIGURATION: PERMANENT ROLES, SQUADS & MINIMUMS
Python
# --- NOBLE ASSIGNMENTS ---
# Name must match CSV exactly. If empty "", the system auto-assigns the best candidate.
PERMANENT_ASSIGNMENTS = {
    'Manager': "'Cypress' Mersethmonom",
    'Broker': "'Cypress' Mersethmonom",
    'Bookkeeper': "'Val' Darłdbim",
    'Chief Medical Dwarf': "",
    'Mayor': "",
    'Militia Commander': "",
    'Captain of the Guard': "",
    'Hammerer': "",
    'Champion': "",
    'Dungeon Master': "",
}

# --- SQUAD DEFINITIONS ---
# id: Squad # | size: members | type: 'melee' or 'marksdwarf'
SQUAD_CONFIG = [
    {'id': 1, 'size': 3, 'type': 'melee'},
    {'id': 2, 'size': 5, 'type': 'marksdwarf'},
]

# --- LABOR MINIMUMS ---
# Forces a minimum number of dwarves into these groups, even if it's not their top skill.
# Priority: [C] > [E] > [D] > [F]
MINIMUM_COUNTS = {
    "[C] HARVESTERS": 4,
    "[D] ARTISANS": 10,
    "[E] SUSTAINMENT": 6,
    "[F] ORDERLIES": 2
}

# --- UNLOCKED ROLES ---
# Add roles here to prevent auto-assignment if you haven't unlocked them in-game yet.
IGNORE_IF_NOT_MANUAL = [
    # 'Hammerer',
    # 'Dungeon Master',
]
[2] DATA PROCESSING LOGIC
Data Sanitization (Anti-Hallucination):

Null Filter: Treat any value in the "Needs" columns greater than 1,000,000 as 0 (removes the 2e9 export artifact).

Skill Floor: Treat any negative skill value (e.g., -1) as 0.

Masonry Alias: Use the highest value among Stone Carver, Stone Crafter, and Stonecutter for all Masonry roles.

Drafting Priority:

Phase 1: Nobles. Assign from PERMANENT_ASSIGNMENTS first, then auto-fill empty roles (if not ignored) using sanitized skills.

Phase 2: Military. Fill SQUAD_CONFIG using the remaining population. Melee: (Axe, Spear, Sword, Mace, Hammer, Fighter). Marksdwarf: (Archer, Crossbowman).

Phase 3: Minimums. Check MINIMUM_COUNTS. If a group is under-staffed, draft the best available remaining dwarves. Active dwarves (those not in recovery) must be prioritized for these slots to ensure the fortress functions.

Phase 4: The Rest. Assign all remaining dwarves to their highest-scoring labor group. If a dwarf has no skills > 0, place in [G] SUPPORT.

Offline Protocol:

A dwarf is "Offline" if any sanitized Need value is negative (e.g., -10,000) or total stress is high.

Laborers/Military: Move to status "Recovering" (Logic: Disable all labors).

Nobles: Remain in Group [A] but mark as "On Break".

[3] REQUIRED OUTPUT
STRICT RULE: Do not save results to .csv files. All tables must be printed directly in the chat response as Markdown tables. Provide:

ADMINISTRATION (NOBLES): Name, Position, Status, and the specific Need causing a break.

MILITARY (SQUADS): Name, Squad ID/Type, Status, and Primary Need.

FORTRESS LABOR: Name, Home Group, Status (Active vs. Recovering), and the Primary Labor category.
